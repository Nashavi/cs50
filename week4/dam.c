

//statically obtain an integer
int x;

//dynamically obtain an integer
int* pk = malloc(sizeof(int));


//get integer form user
int x = get_int();

// array of floats on stack
float stack[x]; 

//array of floats on heap
float* array = malloc(x * sizeof(float));

free();






 
 